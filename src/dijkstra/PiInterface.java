package dijkstra;

public interface PiInterface { //interface de pi
	
	public void setValue(VertexInterface vertex, int value); // fixe valeur d'un sommet
	
	public int getValue(VertexInterface vertex); // renvoie la valeur du sommet
	
	
}
